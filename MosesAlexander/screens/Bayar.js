import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Bayar = () => {
  return (
    <View style={styles.container}>
      <Text>Page Bayar</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default Bayar;
